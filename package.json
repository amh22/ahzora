{
  "name": "ahzora",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ahzora",
    "create-local": "graph create --node http://localhost:8020/ ahzora",
    "remove-local": "graph remove --node http://localhost:8020/ ahzora",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ahzora"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
